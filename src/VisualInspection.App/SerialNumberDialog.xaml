<Window x:Class="VisualInspection.App.SerialNumberDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="录入产品序列号"
        Width="470" Height="240" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" Background="{StaticResource CanvasBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="SerialDialogTitleText" Text="产品序列号" FontSize="18" FontWeight="SemiBold"
                   ToolTip="相机图源每次录入一个序列号，只执行一次检测。" />
        <TextBox x:Name="SerialNumberTextBox" Grid.Row="2" Height="40" Padding="10,7" FontSize="14"
                 KeyDown="SerialNumberTextBox_KeyDown" />
        <TextBlock x:Name="ValidationText" Grid.Row="3" Foreground="{StaticResource FailBrush}" Margin="0,7,0,0" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="88" IsCancel="True" Margin="0,0,10,0" />
            <Button Content="确认并检测" Width="118" Style="{StaticResource PrimaryButton}" Click="Confirm_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
